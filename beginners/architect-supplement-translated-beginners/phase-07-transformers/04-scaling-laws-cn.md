# 缩放定律（Scaling Laws）

> **预计时间：** ~15 分钟  

**更多参数 + 更多数据 + 更多算力 = 更好的模型。** 而且这个关系是可预测的幂律关系。

关键发现（Kaplan 等人，2020）：模型大小、数据集大小、算力——三者需同比例增加。光加参数不加数据 = 欠拟合。

Chinchilla 定律（2022）：**每 1 个参数配 ~20 个 token 的训练数据最优。** 大多数模型"参数太多、数据太少"。

---

> **缩放定律 = 更大模型会更好，但要按比例来。参数、数据、算力三者的关系像三条腿的凳子——少一条就倒。**
