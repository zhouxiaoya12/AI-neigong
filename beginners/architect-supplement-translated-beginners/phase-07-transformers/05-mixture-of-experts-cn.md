# 混合专家（MoE）

> **预计时间：** ~15 分钟  

不是所有参数都在推理时用。MoE 把模型分成多个"专家"，每次推理只激活需要的几个。

```
总参数 671B（DeepSeek-V3）
实际激活 37B（约 5.5%）
每次路由选 8/256 个专家
```

**稀疏激活 = 大模型容量 + 小模型成本。** 关键是路由——"这个问题该派给哪个专家？"——不需要额外负载均衡损失。

---

> **MoE = 大模型的容量，小模型的成本。671B 参数但只用 37B。路由是关键——挑对专家，效率翻倍。**
